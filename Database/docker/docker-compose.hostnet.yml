services:
  beetle-db:
    network_mode: host
    ports: null

  beetle-backend:
    network_mode: host
    ports: null
    environment:
      - DB_HOST=127.0.0.1
      - DB_PORT=3306
      - API_HOST=127.0.0.1
      - API_PORT=8000
