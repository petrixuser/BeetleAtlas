// Beetle Box — lokale Konfiguration (Vorlage)
//
// Diese Datei ist eine Vorlage. Kopiere sie als "config.local.js" und trage
// dort deinen eigenen Google Maps API-Key ein.
//
//   cp config.example.js config.local.js
//
// WICHTIG: config.local.js darf NIEMALS in das Repository eingecheckt werden.
// Sie steht in .gitignore.

window.GMAPS_KEY = "DEIN_API_KEY_HIER";

// Optional: URL zum lokalen Backend (leer lassen fuer Demo-Modus mit Testdaten)
window.API_BASE_URL = "";
