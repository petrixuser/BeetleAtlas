# This file is used to configure pytest for the test suite in this directory. It specifies the test paths, file patterns for test discovery, and additional options for running the tests.

[pytest]
testpaths = .
python_files = test_*.py
addopts = -q
